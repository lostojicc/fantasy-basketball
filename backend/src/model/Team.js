class Team {
    constructor(id, name, creationDate, totalPoints, budget, playerId) {
        this.id = id;
        this.name = name;
        this.creationDate = creationDate;
        this.totalPoints = totalPoints;
        this.budget = budget;
        this.playerId = playerId;
    }
}

export default Team; 